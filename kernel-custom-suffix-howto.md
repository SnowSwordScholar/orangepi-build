# 如何为内核添加自定义后缀

本文档介绍如何在编译内核时添加自定义后缀，使编译出的内核文件名能够包含您指定的个人标识符。

## 配置方法

有两种方式可以设置自定义内核后缀：

### 1. 在配置文件中设置

编辑您的配置文件（例如 `userpatches/config-default.conf`），添加或修改以下行：

```
KERNEL_CUSTOM_SUFFIX="-yourname"    # 将 "yourname" 替换为您想要的标识符
```

注意：
- 建议以连字符（`-`）开头，这样最终的内核版本会是 `-linuxfamily-yourname`
- 不要使用空格或特殊字符，建议只使用字母、数字和连字符

### 2. 在命令行中设置

您也可以在运行 `build.sh` 时通过命令行参数设置自定义后缀：

```bash
./build.sh KERNEL_CUSTOM_SUFFIX="-yourname" ...其他参数...
```

## 效果

设置自定义后缀后，编译出的内核文件名将包含您指定的标识符。例如，如果您设置：

```
KERNEL_CUSTOM_SUFFIX="-john"
```

那么编译出的内核包名称可能会是：
```
linux-image-5.10.60-sunxi-john_21.08.1_arm64.deb
```

而不是默认的：
```
linux-image-5.10.60-sunxi_21.08.1_arm64.deb
```

## 注意事项

1. 自定义后缀会同时应用到内核编译和内核包生成过程中
2. 如果您更改了自定义后缀，建议清理旧的编译缓存：`./build.sh CLEAN_LEVEL="cache,debs"`
3. 过长的后缀可能会导致文件名过长，建议保持简短
4. 此功能不会影响系统的其他部分，只影响内核文件名